server {
    listen 80;
    server_name localhost;

    # Single-page app: unknown paths load index.html. The app calls the backend at VITE_API_URL directly.
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}
